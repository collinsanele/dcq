1. cd to DCQ_CHALLENGE folder

2. create a virtual environment using:
`python -m venv venv`

3. activate virtual environment (for windows) using `venv\Scripts\activate`

4. install dependencies/requirements using `pip install -r requirements.txt`

5. All functionalities are imported and can be used in main.py
   run main.py using `py main.py`
