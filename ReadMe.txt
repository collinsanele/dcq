1. cd to DCQ_CHALLENGE folder

2. activate virtual environment (for windows) using `venv\Scripts\activate`

3. install dependencies/requirements using `pip install -r requirements.txt`

4. All functionalities are imported and can be used in main.py
   run main.py using `py main.py`